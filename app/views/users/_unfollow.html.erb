<% if user_signed_in? && x!= current_user %>
	<%= link_to "UnFollow", unfollow_path(:id => x.id), class: "btn btn-danger", :method => :post, :remote => 'true'%>	
<% end %>
<%= render  partial: "users/followers_following", locals: x: x %>
