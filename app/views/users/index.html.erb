<%= render 'shared/page_title', title: "Our Users" %>



<!--<div class="container">-->
<!--   <#% @users.each_slice(3) do |works| %>-->
<!--   <div class="row"">-->
<!--    <#% @users.each do |x| %>-->
    
<!--        <div class="col-md-4 m-b-lg">-->

<!--          <%#= link_to gravatar_for(x, size: 200), user_path(x) %>-->
<!--           <h5>User name:   </h5>-->
        
<!--        </div>-->
<!--        <%# end %>-->
<!--    </div>-->
<!--    <%# end %>-->
<!--</div>-->

 

<p>
  Download:
  <%= link_to "CSV", users_path(format: "csv") %> |
  <%= link_to "Excel", users_path(format: "xls") %>
</p>


<% @users.each_slice(6) do |works| %>
<div class="container">
<div class="row">
    <% @users.each do |x| %>
        <div class="col-md-4 m-b-lg">
          <div class="panel panel-default panel-profile m-b-0">
            <!--<div class="panel-heading" style="background-image: url(https://images.unsplash.com/photo-1424847651672-bf20a4b0982b?ixlib=rb-0.3.5&q=80&fm=jpg&crop=entropy&s=9bd244971b9cb52a8418cf2f66fce464);"></div>-->
            <div class="panel-heading"></div>
            <div class="panel-body text-center">
                <section class="panel-profile-img">
              <%= image_tag (x.image), class: "gravatar", width: "100", height: "100" %>
              <%#= link_to gravatar_for(x, size: 100), user_path(x) %> 
              </section>
              <h2><%= x.username %></h2> 
              <h2><%= x.email %></h2>            
                <span class="icon icon-add-user"></span> <%= link_to 'View User', user_path(x), class: "btn btn-success btn-small" %>
              </a>
            </div>
            <% if user_signed_in? %>
              <div id="follow_form_<%= x.id %>">
                <% if current_user.following?(x) %>
                  <%= render 'unfollow', x: x %>
                <% else %>
                  <%= render 'follow', x: x %>
                <% end %>
              </div>
            <% end %>
          </div>
        </div>
        <% end %>
      </div>
    <% end %>
  </div>      
  <!--pagination on bttom -->
  <div class="row">
    <div class="col-md-4 col-md-offset-4 center">
    <%= will_paginate %>
    </div>
  </div>
  
